#!/usr/bin/env bash

set -euo pipefail

script_dir=$(dirname -- "$(realpath "${BASH_SOURCE[0]}")")
repo_root=$(dirname -- "$script_dir")

exec cargo run --quiet --manifest-path "$repo_root/Cargo.toml" --bin chess-practice -- "$@"
